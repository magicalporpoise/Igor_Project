/// @description Pressure plates and switches that trigger another action

on = false; //is the trigger active?
key = -1; //what item is needed to make this activate?
          //if the key == 0, check for player interaction
lock = -1; //what door/event does this trigger link to?

inuse = false;

consume = false; // this trigger will consume the item being put into it
stomach = 0;

color = 0;

